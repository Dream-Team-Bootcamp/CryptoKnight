[build]
  command = "npm run build"
  functions = "functions"
  publish = "build"

[dev]
  command = "npm run start"
  functions = "functions-build"
  publish = "build"
  targetPort = 3001


[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
